package com.ctrip.ferriswheel.core.formula;

import com.ctrip.ferriswheel.quarks.Token;

public class OperatorElement extends FormulaElement {
    public OperatorElement(Token token, String operator) {
        super(token, operator);
    }
}
